version: '2'
services:
  haproxy-proxy:
    image: Max-Sum/haproxy-proxy
    container_name: haproxy-proxy
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro

  whoami:
    image: jwilder/whoami
    environment:
      - VIRTUAL_HOST=whoami.local
